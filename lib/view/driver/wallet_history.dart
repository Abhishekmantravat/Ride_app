import 'package:flutter/material.dart';
class wallet_history extends StatelessWidget {
  const wallet_history({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Wallet History"),
      ),
      body: Container(
        child: Column(),
      ),
    );
  }
}